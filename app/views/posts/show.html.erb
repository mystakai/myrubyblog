<h1><%= @post.title %></h1>
<small><%= @post.created_at.strftime("%b. %d, %Y") %></small>
<p><%= @post.body %></p>
<%= link_to "Edit Post", edit_post_path, class: "waves-effect waves-light btn" %>
|
<%= link_to "Delete", @post, data: {confirm: "Are you sure you want to delete?"}, method: :delete,
                                    class: "waves-effect waves-light btn" %>
